import { AppDecimalPipe } from './app-decimal.pipe';

describe('AppDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new AppDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
